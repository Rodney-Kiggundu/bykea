# Loaded for `npm start` (development). Not used in `npm run build`.
# Paynow flows call your Railway API. To use a local Paynow server instead, create `.env.local` with:
#   REACT_APP_SHOP_PAYNOW_LOCAL_URL=http://localhost:4000
REACT_APP_SHOP_PAYNOW_LOCAL_URL=https://bykea-production.up.railway.app
